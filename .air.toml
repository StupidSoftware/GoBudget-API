# .air.toml

root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/server"
  bin = "tmp/main"
  full_bin = "false"
  exclude_dir = ["vendor", "tmp"]

[log]
  time = true

[run]
  cmd = "./tmp/main"
